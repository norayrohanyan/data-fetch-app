import React from 'react';
import AppRouter from 'pages/Router';

const App: React.FC = () => {
  return (
    <React.StrictMode>
      <AppRouter />
    </React.StrictMode>
  );
};

export default App;